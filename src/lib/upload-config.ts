/** Máximo de archivos que se suben simultáneamente desde el browser. */
export const UPLOAD_CONCURRENCY_LIMIT = 5;

/** Máximo de pipelines de IA que corren simultáneamente en el servidor. */
export const PROCESS_CONCURRENCY_LIMIT = 3;

/** Máximo de archivos PDF por lote. */
export const MAX_FILES = 50;

/** Tamaño máximo de archivo en bytes (20 MB). */
export const MAX_FILE_SIZE_BYTES = 20 * 1024 * 1024;
